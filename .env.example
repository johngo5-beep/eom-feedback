# Environment variables for local development and Vercel.
# Copy to `.env.local` and fill in values. Never commit secrets.

# Node environment (set automatically by Next.js / Vercel)
# NODE_ENV=development

# Database connection string (add when persistence is configured)
# DATABASE_URL=

# Secret used to protect the admin area (add when auth is configured)
# ADMIN_SECRET=
